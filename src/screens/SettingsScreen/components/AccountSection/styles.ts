import { StyleSheet } from "react-native";

export default StyleSheet.create({
  section: {
    padding: 20,
    borderRadius: 12,
    marginBottom: 16,
  },
  accountRow: {
    paddingVertical: 12,
    borderBottomWidth: 1,
    gap: 4,
  },
  logoutButton: {
    marginTop: 16,
    paddingVertical: 14,
    borderRadius: 12,
    borderWidth: 2,
    alignItems: "center",
    justifyContent: "center",
  },
});
