import { StyleSheet } from "react-native";

export default StyleSheet.create({
  logo: {
    borderRadius: 16,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 16,
  },
  logoIcon: {},
});
