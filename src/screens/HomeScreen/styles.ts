import { StyleSheet } from "react-native";

export default StyleSheet.create({
  scrollView: {
    flex: 1,
  },
  content: {
    paddingBottom: 32,
  },
  cards: {
    padding: 16,
    gap: 12,
  },
});
