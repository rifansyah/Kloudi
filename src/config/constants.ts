export const APP_NAME = "Kloudi";
export const DEFAULT_LANGUAGE = "en";
export const SUPPORTED_LANGUAGES = ["en", "id", "my"] as const;
export const THEME_MODES = ["light", "dark", "system"] as const;
